name: first workflow
on: workflow_dispatch
jobs:
  first-job:
    runs-on: ubuntu-latest
    steps:
      - name: Print Greeting
        run: |
            echo "First output!"
            echo "Second output!"
            echo "Third output!"
      - name: Good bye
        run: echo "Good bye"
      
